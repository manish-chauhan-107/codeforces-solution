#include <bits/stdc++.h>
using namespace std;
 
int main() {
   int t ;
   string a;
   cin>>t;
   vector<int> v(6);
   while(t--){
   cin>>a;
   for (int i = 0 ; i < 6 ; i++){
      v[i] = a[i];
   }
   if(v[0]+v[1]+v[2] == v[3]+v[4]+v[5]){
      cout<<"YES"<<endl;
   }
   else{
      cout<<"NO"<<endl;
   }
}
   return 0;
}