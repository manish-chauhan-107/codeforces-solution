#include <bits/stdc++.h>
using namespace std;
 
int main() {
  set<long long> x;
  for(int i = 0 ; i < 4 ; i++){
     int m ;
     cin>>m;
     x.insert(m);
  }
  int n = x.size();
  cout<<(abs(n-4));
  return 0;
}